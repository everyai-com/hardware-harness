# Workers AI model used for generation. Swap freely — see
# https://developers.cloudflare.com/workers-ai/models/
AI_MODEL="@cf/zai-org/glm-5.3-flash"

# Workers AI model used for design reference renders (text-to-image, multipart input).
AI_IMAGE_MODEL="@cf/black-forest-labs/flux-2-klein-9b"

# Copy to .dev.vars for local dev. In production, set these as Worker secrets/vars:
#   npx wrangler secret put AI_MODEL
